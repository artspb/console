Программа для расчета вероятностных характеристик показателей качества процессов переработки контейнерных грузов.

Параметры вызова программы: console S m phi [nu] [acc]
S - число причалов (S > 0)
m - число судов на маршруте (m > 0)
phi - коэффициент загрузки причала (0 < phi < 1)
nu - коэффициент потока с первым приоритетом (0 < nu < 1), по-умолчанию nu = 0
acc - точность (acc > 0), по-умолчанию acc = 1e-8

Примеры вызова программы:
console 5 100 0.7
console 5 100 0.7 0 1e-10 //Вывод исходных данных аналогичен предыдущему вызову, но рассчеты ведутся с большей точностью.
console 3 50 0.85 0.3 //Первый поток обладает абсолютным приоритетом и составляет 30% от всего потока судов.

Если введенные данные не удовлетворяют условиям, вывод программы будет пустым.
Если данные будут введены корректно, программа выведет в терминал полученные данные:
среднее приведенное временя ожидания, среднее суммарное приведенное временя ожидания,
число судов в очереди, число судов в терминале и число итераций. Причем для случая без приоритета
вывод будет содержать одну строку с данными, а для случая с приоритетом – три
(средние значения, значения для первого и второго потока).
Если данные, введенные пользователем, физически несовместимы, программа выдаст вывод,
состоящий из чисел -1 (вне зависимости от дисциплины обработки).

Хвастунов Артем, 2010 год. СПбГУВК.
